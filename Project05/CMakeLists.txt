CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

PROJECT(AVI_FILE)

SET(CMAKE_EXPORT_COMPILE_COMMANDS ON)

FIND_PACKAGE(OpenCV REQUIRED)

INCLUDE_DIRECTORIES(
    include
    ${OpenCV_INCLUDE_DIRS}
)

ADD_EXECUTABLE(AVI_FILE src/main.cpp)

TARGET_LINK_LIBRARIES(AVI_FILE ${OpenCV_LIBS})